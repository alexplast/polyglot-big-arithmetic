# Предстоящие задачи (TODO)

Список задач по улучшению и расширению бенчмарка производительности.

### 1. Изоляция и воспроизводимость
- [ ] **Docker-окружение**: Создать `Dockerfile` со всеми необходимыми компиляторами (GCC, GFortran, Go, Rust, JDK, Node.js, Python) фиксированных версий, чтобы исключить влияние разницы в системном ПО на результаты.
- [ ] **Автоматизация сборки**: Обновить `Makefile` для поддержки запуска всех тестов внутри Docker-контейнера одной командой.

### 2. Точность измерений
- [ ] **Стандартизация данных для Sort**: Вынести генерацию случайных чисел в отдельный скрипт `tests/generate_data.py`. Сохранять массив в бинарный файл, чтобы каждый язык сортировал идентичный набор данных, исключая накладные расходы на LCG внутри замера времени.
- [ ] **Статистический анализ**: Увеличить количество прогонов и добавить расчет стандартного отклонения в CSV-отчеты.

### 3. Расширение алгоритмов BigInt
- [ ] **Manual vs Native**: Добавить во все языки (где это возможно) "честную" ручную реализацию BigInt на базе вектора (Base 10^9), чтобы сравнить эффективность реализации массивов в разных рантаймах.
- [ ] **GMP/External Libs**: Добавить поддержку промышленных библиотек (например, GMP для C/C++, `num-bigint` для Rust) для сравнения с нативными BigInt из Go/Java.

### 4. Низкоуровневые оптимизации
- [ ] **Assembler (AVX2)**: Обновить реализацию матричного умножения в ASM с использованием 256-битных регистров AVX2 (`vmovupd`, `vfmadd231pd`), чтобы показать теоретический предел производительности процессора.
- [ ] **SIMD в Rust/C++**: Проверить влияние флагов `-march=native` и явных интринсиков на результаты матричных вычислений.

### 5. Визуализация и отчетность
- [ ] **Генерация графиков**: Создать скрипт `tests/plot_results.py` (на базе `matplotlib` или `plotly`), который будет автоматически генерировать PNG-графики по результатам CSV-файлов.
- [ ] **Логарифмическая шкала**: Использовать логарифмическую шкалу для графиков, чтобы наглядно отображать разницу между интерпретируемыми (Python) и компилируемыми языками.

### 6. Рефакторинг кода
- [ ] **Оптимизация Makefile**: Избавиться от дублирования правил компиляции, используя шаблоны.
- [ ] **Унификация тестов**: Объединить разрозненные скрипты `tests/benchmark_*.py` в единый мощный инструмент с поддержкой аргументов командной строки.
